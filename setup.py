from setuptools import setup, find_packages

setup(
    name='MODM_Tool_Modules',
    version='1.0',
    packages=find_packages(),
    # Add any other package dependencies here
    install_requires=[
        # 'somepackage>=1.0.0',
    ],
)
